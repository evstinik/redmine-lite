# ──────────────────────────────────────────────────────────────────────────────
# Redmine Lite — environment variables template
# Copy this file to .env.local and fill in the values.
# ──────────────────────────────────────────────────────────────────────────────

# ── Redmine ───────────────────────────────────────────────────────────────────

# Full URL of your Redmine instance, without a trailing slash.
# Used by the Vite dev server to proxy /api requests.
# Example: https://redmine.example.com
VITE_REDMINE_URL=

# URL subpath the frontend uses as the API prefix, without a trailing slash.
# The dev proxy strips this prefix before forwarding to Redmine.
# Example: /api
VITE_API_URL=/api

# ── Jira ──────────────────────────────────────────────────────────────────────

# Full URL of your Jira instance, without a trailing slash.
# Used by the Vite dev server to proxy /jira-api requests.
# Example: https://jira.example.com
VITE_JIRA_URL=

# ── Tracker IDs (optional) ────────────────────────────────────────────────────

# Redmine tracker ID that maps to "User Story". Defaults to 14 if not set.
# VITE_TRACKER_USER_STORY_ID=14

# Redmine tracker ID that maps to "Bug". Defaults to 1 if not set.
# VITE_TRACKER_BUG_ID=1

# ── Scripts (yarn timelog:export) ─────────────────────────────────────────────

# Full URL of your Redmine instance used by CLI scripts.
# Can be set to the same value as VITE_REDMINE_URL.
REDMINE_URL=

# Personal access token for authenticating script requests to Redmine.
# Generate one on your Redmine profile page (right panel → API access key).
SCRIPT_REDMINE_ACCESS_TOKEN=

# (Optional) Project identifier or numeric ID to filter exported time entries.
# If not set, the script will prompt for it interactively.
# SCRIPT_REDMINE_PROJECT_ID=

# (Optional) Date range for the time entries export (inclusive), format: YYYY-MM-DD,YYYY-MM-DD.
# If not set, the script will prompt for it interactively.
# SCRIPT_REDMINE_TIME_SPAN=
